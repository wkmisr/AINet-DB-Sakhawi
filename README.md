# Arab-Islam Network DB Project (AINet-DB)

本リポジトリは、アル＝サハーウィー(al-Sakhāwī, d. 902 H / 1497 CE)の『輝く光(*al-Ḍawʾ al-Lāmiʿ li-Ahl al-Qarn al-Tāsiʿ*)』を構造化し、マムルーク朝後期(9世紀ヒジュラ)の知識ネットワークを再構築するためのデジタル人文学プロジェクトです。

## プロジェクトの概要

- **対象資料**: 9世紀ヒジュラ暦の人物録(約 13,000 名の伝記エントリを収録)
- **手法**: 生成 AI(Gemini / Claude)による構造化抽出 + 専門家による校閲
- **目標**: TEI XML 形式でのデータベース化、Wikidata / GeoNames 等の外部権威ファイルとの連携、人的ネットワークの可視化
- **現行バージョン**: **v20.10**(2026-05-13)

---

## プロジェクトのデータセット

| ファイル / リソース | 概要 |
|---|---|
| **`0_DawForAIND_Cleaned.txt`** | 『輝く光』の本文(クリーン化済み、AIND-D ID 付与済み)。アプリへの入力にはこのテキストを使用 |
| **ID-Master**(Google スプレッドシート) | 地名・人物・機関・書物・学問分野などの ID を一元管理するマスターリスト。解析時にアプリが自動参照 |
| **Sakhawi_PersonList**(Google スプレッドシート) | 『輝く光』に立項されている人物の一覧。担当者の進捗管理・解析結果のレビューに使用 |

### 本文テキストの形式

`0_DawForAIND_Cleaned.txt` は次の形式で各エントリを含みます:

```
###$AIND-DXXXXX | NNNNNNNNNNNN$# {マーカー} 本文…
```

- **AIND-DXXXXX**: AIND-D ID(5 桁、xml:id として使用)
- **NNNNNNNNNNNN**: 12 桁数字の元データ ID(source 属性として使用)
- **マーカー**:
  - `$` 男性立項
  - `$$` 女性立項
  - `$$$` 相互参照(他の項目への言及)
  - `|` `||` 補助マーカー(複合エントリ等)

例:
```
###$AIND-D00001 | 932540579843$# $ آدم بن سعد بن عيسى الكيلاني الأصل ثم المكي
```

全 15,530 エントリ、うち立項 13,440 件。

---

## 識別子の体系(本プロジェクトの中核)

本プロジェクトでは **2 つの主要識別子** を使い分けます。

| 識別子 | 形式 | 例 | 役割 | データソース |
|---|---|---|---|---|
| **AIND-D ID** | `AIND-DXXXXX`(5 桁) | `AIND-D00001` | XML の `xml:id` 属性として使用、TEI の不変キー | テキストヘッダーから取得 |
| **12 digits ID** | 12 桁数字 | `932540579843` | XML の `source` 属性として使用、元データの自然キー | テキストヘッダーから取得 |

### XMLでの使われ方

```xml
<person xml:id="AIND-D00001" source="932540579843">
    <!-- generated by AINet-DB Researcher Pro v20.10 (2026-05-13) at 2026-05-13T... -->
    <persName type="full" xml:lang="ar">آدم بن سعد بن عيسى الكيلاني...</persName>
    ...
</person>
```

- **xml:id**: TEI 内部での参照キー。他のエントリから `passive="#AIND-D00001"` のように参照される
- **source**: 元データへの参照。データの追跡可能性を確保
- **XML コメント**(v20.5 以降): どのバージョンのアプリで生成されたかを記録(後の検証用)

### XMLファイル名の規約

ダウンロード XML のファイル名は次の形式:

```
{AIND-D ID}_{12 digits ID}.xml
```

例: `AIND-D00001_932540579843.xml`

---

# AINet-DB Researcher Pro 使い方ガイド

**対象**: デジタルツールに不慣れな人文学研究者・アシスタント

**目的**: イスラーム伝記史料から人物データを抽出し、データベース用の XML ファイルを作成する

## このツールでできること

アラビア語の人名録(伝記史料)のテキストを貼り付けると、AI が自動的に人物情報を読み取り、整理してくれます。研究者はその結果を確認・修正しながら、学術データベース用のフォーマット(TEI-XML)を作成できます。また、作成したデータは共有のスプレッドシートに保存することができます。

## 画面の構成

画面は大きく **左側(サイドバー)** と **右側(メインエリア)** に分かれています。

- **左側**: 史料テキストの入力・解析・翻訳の表示
- **右側**: AI が抽出したデータの確認・修正・XML 出力・保存

メインエリアは縦に長いフォームになっており、上から順に以下のセクションが並びます。

1. **基本情報**(AIND-D ID、12 digits ID、性別、persName 等)
2. **ニスバ・ラカブ**(別名)
3. **Birth / Death**(生没年・**生地・没地・埋葬地** v20.1 以降)
4. **⚖️ Madhhab**(法学派、**複数対応** v20.8 以降)
5. **☪️ Sufi Order**(スーフィー教団)
6. **🎓 Teachers**(師匠)
7. **🧑‍🎓 Students**(弟子)
8. **📍 Activities**(地理的活動)
9. **🏛️ Institutions**(所属機関)
10. **🏅 Offices**(役職)
11. **👨‍👩‍👧 Family Relations**(家族関係)
12. **🤝 Social Relations**(社会関係)
13. **📅 Biographical Events**(伝記的出来事)
14. **メモ**(Person Notes / Editors' Notes / 翻訳)
15. **🖊️ respStmt**(作業者情報)

## 作業の流れ

### ▶ ステップ 1: 史料テキストを入力する

画面左側(サイドバー)の「Source Text」エリアに、解析したい人物の伝記テキストをアラビア語で貼り付けます。

**重要**: 一度に入力するのは **人物 1 人分** のテキストにしてください。複数人を同時に入力すると、AI が混乱して正しく抽出できません。

テキストには `###$AIND-DXXXXX | NNNNNNNNNNNN$#` のヘッダーを含めてください。これによりアプリが AIND-D ID と 12 digits ID を自動抽出します。

### ▶ ステップ 2: 解析する

「🔍 解析する」ボタンを押すと、Gemini API が起動して伝記テキストを解析します。数秒〜数十秒待つと、メインエリアの各フィールドに自動的に抽出結果が反映されます。

### ▶ ステップ 3: データを確認・修正する

AI が抽出した内容を、研究者の目で確認します。間違いや漏れがあれば、各フィールドを直接編集して修正します。

**便利機能**:

- **▲▼ ボタン**: Teachers / Students / Activities / Institutions / Offices などの各エントリを上下に並べ替えできます。時系列順に整理する際に便利です。
- **❌ ボタン**: 不要なエントリを削除します。
- **＋ add ボタン**: 新しいエントリを追加します。
- **🗑️ 入力をクリア ボタン**: 画面右上にあり、入力内容を一括でクリアできます。誤クリック防止のため 2 段階確認があります。
- **↗ 翻字を一括補完 ボタン**: 空のラテン欄(ラテン文字翻字)を IJMES 規格で一括補完します(既存の入力は保持)。
- **🔎 ID-Master 照合 ボタン**: アラビア語の名前を ID-Master と完全一致照合し、未確定の ID 欄に自動で値を入れます。
- **🔢 採番を更新 ボタン**: プレースホルダー(`TMP-X-00000` 等)を空き番号で自動採番します。**v20.9 以降**: 名前欄が空欄の場合は採番せず、孤児 ID(名前なしの ID だけ)が生成されないよう保護されます。

### ▶ ステップ 4: XML を確認・ダウンロードする

メインエリアの「TEI-XML Export」セクションに自動生成された XML が表示されます。

- **📥 XML をダウンロード ボタン**: `{AIND-D ID}_{12 digits ID}.xml` 形式のファイルとして 1 クリックで保存できます。
- **📋 XML をクリップボードにコピー ボタン**: XML 全体をクリップボードに保存します。

### ▶ ステップ 5: スプレッドシートに保存する

最終的なデータは「📤 スプレッドシートに保存」ボタンで共有スプレッドシート(Sakhawi_PersonList)に保存できます。

**v20.10 以降の変更**: 担当者(初版作成者)は respStmt セクションから **自動取得** されます。スプレッドシート保存セクションには「📌 **担当者(初版作成者): XXX**」と表示されるので、確認してから保存してください。respStmt が空の場合、保存ボタンを押すとエラーが表示されます — その場合、respStmt セクションで担当者を追加してから保存してください。

---

## 入力フィールドの詳細

### 基本情報

| フィールド | 説明 | 例 |
|---|---|---|
| **AIND-D ID (xml:id)** | テキストヘッダーから自動抽出。XML の xml:id として使用 | `AIND-D00001` |
| **12 digits ID (@source)** | テキストヘッダーから自動抽出。XML の source 属性として使用 | `932540579843` |
| **Sex** | 性別 | M(男性)/ F(女性)/ U(不明) |
| **persName (Full Arabic)** | 法学派まで含めたフルネーム | إبراهيم بن أحمد ... الشافعي |
| **persName (Ism/Father/GF)** | 本名 + 父 + 祖父 | إبراهيم بن أحمد بن عبد الكافي |

### Birth / Death(v20.1 以降の拡張)

ヒジュラ暦の年だけでなく、年月日・**場所(生地・没地・埋葬地)** も入力できます。

#### Birth 欄

| フィールド | 説明 | 例 |
|---|---|---|
| Birth (H) | ヒジュラ暦 | `850` / `850-09` / `850-09-15` |
| Birth (G) | 西暦(自動計算、参考用) | `1446` |
| Birth Cert | 確実性 | high / medium / low / 未指定 |
| Birth Note | 注記 | "Ca. 850" / "異説あり(901)" |
| Birth Inference Note | 推論の根拠(英語推奨) | 'Inferred from "died at 50 in 900H".' |
| **📍 Birth Place (Arabic)** | 生地のアラビア語名 | `القاهرة` |
| **Birth Place (Latin)** | IJMES 翻字 | `al-Qāhira` |
| **Birth Place ID** | GeoNames 数字 ID または TMP-L- | `360630` |

#### Death 欄

Birth と同じ構成。さらに **Burial Place(埋葬地)** が独立して入力可能:

| フィールド | 説明 | 例 |
|---|---|---|
| **📍 Death Place** | 没地(`مات بـ X` / `المتوفى بها` から抽出) | `مكة` / `Makka` |
| **⚰️ Burial Place** | 埋葬地(`دفن بـ X` から抽出) | `المعلاة` / `al-Muʿallā` |

#### XML での出力例

```xml
<death when-custom="0887-12-05" when="1483-01-24" cert="high">
    <placeName xml:lang="ar" ref="gn:104515">مكة</placeName>
    <placeName xml:lang="ar-Latn" ref="gn:104515">Makka</placeName>
    <placeName xml:lang="ar" type="burial" ref="wd:Q42004">المعلاة</placeName>
    <placeName xml:lang="ar-Latn" type="burial" ref="wd:Q42004">al-Muʿallā</placeName>
    <note xml:lang="en">Funeral prayer was held the day after his death.</note>
</death>
```

葬儀の祈り(`صلى عليه`)は `<death>` 内の `<note>` として統合されます。**独立した bio_event として記録しません**(v20.2 以降の仕様)。

### Madhhab(法学派、v20.8 で複数対応)

通常は単一の法学派ですが、転向した人物(例: シャーフィイー派 → ハンバリー派)に対応するため、**最大 3 つまで** 時系列順に記録できます。

- **Seq**: 順番(1, 2, 3)
- **Madhhab**: プルダウンから選択(Hanafi / Maliki / Shafi'i / Hanbali / Unknown / Other)
- **Wikidata ID**: 自動表示
- **❌ ボタン**: 個別削除
- **＋ Madhhab を追加 ボタン**: 行を追加(最大 3 つまで)

#### XML 出力

```xml
<!-- 単一の場合(n 属性なし) -->
<affiliation type="madhhab" ref="wd:Q160851">Hanafi</affiliation>

<!-- 複数(時系列順、n 属性付き) -->
<affiliation type="madhhab" n="1" ref="wd:Q82245">Shafi'i</affiliation>
<affiliation type="madhhab" n="2" ref="wd:Q170446">Hanbali</affiliation>
```

スプレッドシート I 列には `Shafi'i → Hanbali`(矢印で連結)として保存されます。

### Teachers / Students

各エントリは 4 行構成です。

- **1 行目**: Name(師匠/弟子の名前)+ Person ID
- **2 行目**: 
  - **Method**(学習方法): 動詞(سمع, أخذ, قرأ 等)。プルダウン or 自由記述
  - **Field**(学習分野): 学問分野(الفقه, الحديث 等)。プルダウン or 自由記述
- **3 行目**: Text Arabic / Text Latinized / Text ID(学んだ書物)
- **4 行目**: Date / Place(学んだ年・場所)

**v20.9 以降の改善**: テキストが言及されていない場合(Text Arabic が空欄)、Text ID も空欄のまま保持されます。これまで「Text ID だけ番号が入る」という孤児 ID 問題がありましたが、解消されました。

▲▼ ボタンで時系列順に並べ替えできます。

### Activities

地理的活動(移住・訪問・巡礼など)を記録します。Type:

| Type | 説明 |
|---|---|
| residence | 居住 |
| visit / travel | 訪問・移動 |
| study | 遊学・学問のための移動 |
| **hajj / umrah** | 大巡礼・小巡礼 |
| jāwara | メッカ/メディナでの長期滞在(مجاورة) |
| riḥla | 学問的旅行(الرحلة) |
| legacy | 死後の遺産・影響 |
| other | その他 |

**v20.1 以降の変更**: `buried`(埋葬)タイプは **廃止** されました。埋葬地は Death 欄の Burial Place で管理します(過去データは自動マイグレーション済み)。

### Family Relations

家族関係を記録します。relation の種類は以下の 16 種類:

- **直系**: father, mother, son, daughter
- **横系**: brother, sister, spouse
- **祖父母**: grandfather, grandmother
- **傍系**: uncle, aunt, cousin, siblings_child(甥・姪)
- **系譜**: ancestor(先祖), descendant(子孫)
- **その他**: other

**重要**: Family は **史料に明示的に言及された関係のみ** 記録します。nasab 連鎖(`محمد بن أحمد` から父・祖父を自動展開)はしません。kunyah(`أبو فلان`)から息子を推測することもしません。

### Biographical Events

**v20.2 以降の運用方針**: bio_events は **具体的な日時のあるイベント** のみ記録します。描写的な記述(「学者から学んだ」「美しく朗誦した」など)は記録しません。

| Type | 説明 | 例 |
|---|---|---|
| political | 政治的事件 | 出陣、政争への関与、特定年の投獄 |
| cultural | 著作・知的活動 | 著作の完成、講義 |
| religious | 宗教的事件 | 改宗、霊的体験、ビジョン |
| other | その他 | 疫病罹患、自然災害 |

**重要**:
- ハッジ(巡礼)は Activities に記録します。Bio Events には含めません。
- 葬儀の祈り(`صلى عليه`)は Bio Events ではなく `<death>` 内の note に統合されます(v20.2 以降)。
- 「学者から継続的に学んだ」のような **描写的記述** は bio_events には記録しません(teachers/relations で記録されるため重複)。

### Social Relations

家族でも師弟でもない社会的関係を記録します。

| Type | 説明 |
|---|---|
| patron | 庇護者 |
| client | 被庇護者 |
| colleague | 同僚 |
| rival | 論敵 |
| friend | 友人 |
| correspondent | 書簡相手 |
| successor / predecessor | 後継者・前任者 |
| other | その他 |

### respStmt(作業者情報)

この `person` 要素の編集に関わった担当者を記録します。複数の作業履歴を並列で記録可能。

**v20.10 以降の重要な変更**: respStmt の **最初のエントリの作業者名** が、スプレッドシート B列の「担当者」として **自動的に使用** されます。担当者を 2 回入力する必要はありません。

役割の選択肢:

- 初版作成(スプレッドシート B列に使われる役割)
- 修正・追記
- 校閲
- 翻訳
- ID 照合
- その他

---

## ID の体系

このツールでは、人物・地名・機関などに外部データベースの ID を付与します。

| 種類 | 用途 | 例 |
|---|---|---|
| **GeoNames ID**(数字のみ) | 地名 | `104515`(メッカ) |
| **Wikidata Q-ID**(`Q` 始まり) | 概念・組織・人物・書物 | `Q160851`(ハナフィー派) |
| **TMP-P-**(仮 ID、6 桁) | 外部 ID 未確定の人物 | `TMP-P-000001` |
| **TMP-N-**(仮 ID、5 桁) | ニスバ | `TMP-N-00001` |
| **TMP-L-**(仮 ID、5 桁) | 地名 | `TMP-L-00001` |
| **TMP-I-**(仮 ID、5 桁) | 機関 | `TMP-I-00001` |
| **TMP-O-**(仮 ID、5 桁) | 役職 | `TMP-O-00001` |
| **TMP-T-**(仮 ID、5 桁) | 書物・典籍 | `TMP-T-00001` |
| **TMP-S-**(仮 ID、5 桁) | 学習方法・分野 | `TMP-S-00001` |

TMP-(仮 ID)は後から正式な ID に差し替えることができます。

### v20.9 以降の保護機能

**孤児 ID の自動防止**: 名前(アラビア語)が空欄の場合、対応する ID も空欄にクリアされます。これにより、本文に言及がないのに ID だけ採番される現象を防ぎます。

例:
- 本文に書物の言及なし → `text_ar` = 空欄、`text_id` = 空欄(以前は `TMP-T-00031` 等が誤って入っていた)

### 出力 XML での参照形式

XML 内では次のプレフィックスで参照を区別します:

| プレフィックス | 種類 | 例 |
|---|---|---|
| `wd:` | Wikidata | `wd:Q160851` |
| `gn:` | GeoNames | `gn:104515` |
| `#` | プロジェクト内参照(TMP-, AIND-D) | `#TMP-P-000001`, `#AIND-D00033` |

---

## ID Master について

画面左側の「📋 ID Master 状態」を開くと、研究グループが共有している ID リストを確認できます。このリストに登録されている地名・機関・人物・学習方法・学習分野は、解析時に AI が自動的に正しい ID を割り当てます。

Method(学習方法)と Field(学習分野)のプルダウンは ID-Master から動的に語彙を読み込みます。ID-Master に新しい語彙が追加されると、アプリも自動で反映します。

### ID Master に新規 ID を登録する方法

史料を読んでいると、まだ ID Master に登録されていない地名・機関・人物・書物などが出てきます。そのような場合、外部データベースで ID を調べて ID Master に追加します。

#### ① 地名の場合 → GeoNames で調べる

1. https://www.geonames.org/ を開く
2. 地名を検索(英語かアラビア語)
3. ヒットした地名のページで URL の数字部分を取得(例: `https://www.geonames.org/104515/` → ID は `104515`)
4. ID Master スプレッドシートに追加:
   - Category: `Place`
   - Arabic: アラビア語表記
   - Latin: ラテン文字表記
   - ID: GeoNames の数字

#### ② 機関・概念・法学派・スーフィー教団の場合 → Wikidata で調べる

1. https://www.wikidata.org/ を開く
2. 名称で検索
3. ヒットした項目の URL から Q-ID を取得(例: `https://www.wikidata.org/wiki/Q160851` → ID は `Q160851`)
4. ID Master スプレッドシートに追加:
   - Category: `Institution` / `Concept` 等
   - ID: `Q` から始まる Wikidata の ID

#### ③ 人物の場合 → Wikidata で調べる

著名な人物(Ibn Ḥajar al-ʿAsqalānī = `Q471116` 等)は Wikidata に登録されています。同上の方法で Q-ID を取得。

#### ④ 書物・典籍の場合 → Wikidata で調べる

主要なハディース集等は Wikidata に登録があります:

| 書物 | Q-ID |
|---|---|
| al-Bukhārī の Ṣaḥīḥ | `Q208507` |
| Muslim の Ṣaḥīḥ | `Q193272` |
| al-Tirmidhī の Sunan | `Q1248893` |
| Abū Dāwūd の Sunan | `Q593290` |
| al-Nasāʾī の Sunan | `Q1140365` |
| Ibn Mājah の Sunan | `Q940817` |
| Mālik の Muwaṭṭaʾ | `Q900871` |
| al-Dāraquṭnī の Sunan | `Q12217063` |

#### ⑤ 見つからない場合は仮 ID のままでよい

外部データベースに該当が見つからない場合、TMP- 仮 ID(例: `TMP-P-000001`)のままで構いません。アプリの「🔢 採番を更新」ボタンで自動採番されます。

後日、正式な Wikidata 項目が作成されたら、ID Master を更新することで全エントリに反映されます。

---

## スプレッドシート運用

### Sakhawi_PersonList の列構成

| 列 | 内容 | アプリの動作 |
|---|---|---|
| **A** | 行数 | 触らない(関数式があれば保持) |
| **B** | 担当者 | **書き込む(v20.10 以降: respStmt から自動取得)** |
| **C** | AIND-D ID(`AIND-DXXXXX`) | **触らない**(手動記入領域) |
| **D** | 12 digits ID | **触らない**(検索キー、手動記入領域) |
| **E** | persName (Full Arabic) | 書き込む |
| **F** | persName (Ism/Father/GF) | 書き込む |
| **G** | Birth (H) | 書き込む |
| **H** | Death (H) | 書き込む |
| **I** | Madhhab(複数の場合は `→` で連結) | 書き込む |
| **J** | Editors' Notes | 書き込む |

### C列・D列を「触らない」運用の理由

- AIND-D ID と 12 digits ID は元データに由来する不変キーであり、プロジェクト管理者が管理する
- アプリが書き換えるとデータの整合性が崩れる可能性があるため、安全のため手動記入領域とする
- 新規行追加時も、アプリは B / E〜J 列のみに書き込み、C / D 列は空のまま残す(後で人間が記入)

### 既存行更新と新規行追加

- **既存行更新**: D 列(12 digits ID)で検索し、一致する行があれば B 列と E〜J 列を更新
- **新規行追加**: 一致する行がない場合、D 列が空の最初の行を探し、その行の B / E〜J 列に書き込む(C / D 列は手動記入のため空のまま)
  - 保存後、画面に「C 列・D 列に AIND-D ID と 12 digits ID を手動で記入してください」と表示される

---

## XML 出力

### 出力される XML の構造(v20.10 仕様)

```xml
<person xml:id="AIND-D00001" source="932540579843">
    <!-- generated by AINet-DB Researcher Pro v20.10 (2026-05-13) at 2026-05-13T... -->
    <persName type="full" xml:lang="ar">آدم بن سعد بن عيسى الكيلاني...</persName>
    <persName type="name_only" xml:lang="ar">آدم بن سعد بن عيسى</persName>
    <persName type="nisbah" xml:lang="ar" ref="...">الكيلاني</persName>
    <persName type="laqab" xml:lang="ar">...</persName>
    <sex value="M"/>
    <affiliation type="madhhab" ref="wd:Q82245">Shafi'i</affiliation>
    <birth when-custom="0850" when="1446" cert="high">
        <placeName xml:lang="ar" ref="gn:104515">مكة</placeName>
        <placeName xml:lang="ar-Latn" ref="gn:104515">Makka</placeName>
    </birth>
    <death when-custom="0902" when="1497" cert="high">
        <placeName xml:lang="ar" ref="gn:360630">القاهرة</placeName>
        <placeName xml:lang="ar-Latn" ref="gn:360630">al-Qāhira</placeName>
        <placeName xml:lang="ar" type="burial" ref="wd:Q42004">المعلاة</placeName>
        <placeName xml:lang="ar-Latn" type="burial" ref="wd:Q42004">al-Muʿallā</placeName>
    </death>
    <listRelation>
        <relation type="personal" subtype="teacher" n="1" 
                  active="#{師匠のID}" passive="#AIND-D00001">
            <desc type="method" ref="..." xml:lang="ar">سمع</desc>
            <desc type="field" ref="..." xml:lang="ar">الحديث</desc>
        </relation>
        ...
    </listRelation>
    <event type="residence" when-custom="0870">
        <placeName ref="gn:104515">مكة</placeName>
    </event>
    ...
    <respStmt>
        <resp xml:lang="ja">初版作成</resp>
        <persName>Erina Ota</persName>
        <date when="2026-05-11"/>
    </respStmt>
</person>
```

### XML 出力の設計方針

- **xml:id は `AIND-DXXXXX` 形式**(5 桁)、TEI の不変キー
- **source 属性に 12 桁数字**を保持(元データへの参照を明示)
- **XML コメントでバージョン記録**(v20.5 以降): 生成元のアプリバージョンと日時を埋め込み
- **日付は `when-custom` でヒジュラ暦、`when` で西暦** を併記
- **言語タグは厳密に**: アラビア語 `xml:lang="ar"`、ラテン文字翻字 `xml:lang="ar-Latn"`、日本語 `ja`、英語 `en`
- **推論注記** `<note type="inference">` で推論の根拠を記録
- **birth/death 内の placeName**(v20.1 以降): 生地・没地・埋葬地を birth/death 要素内に統合(`type="burial"` で埋葬地を区別)

---

## データ品質と運用方針

### 抽出時の重要原則

#### 1. nasab 連鎖から家族関係を自動展開しない

`محمد بن أحمد بن علي بن حسن` のような nasab(系譜)連鎖から、自動的に「父=أحمد、祖父=علي」のように展開してはいけません。Family Relations は **史料が明示的に言及した関係のみ** 記録します。nasab 自体は `full_name` / `name_only` に保存されています。

#### 2. ancestor のタイトルを subject に流用しない

`إبراهيم بن العلامة محمد` の場合、`العلامة`(博学者の意)は **父のタイトル** であり、subject(إبراهيم)のタイトルではありません。subject のタイトル(laqab/honorific)に組み入れないこと。

#### 3. 法学派は nisbah ではなく madhhab に

`الشافعي` は nisbah(地名・家系由来の名)ではなく madhhab(法学派)です。`madhhabs` フィールドに記入し、nisbahs には記入しません。

#### 4. ハッジは「حج」「حجّ」と明記された場合のみ

`ذهب إلى مكة`(メッカに行った)は visit。`حج سنة كذا`(年に巡礼した)は hajj。曖昧な場合は visit を選びます。

#### 5. 集合代名詞は記録しない

`وغيره / جماعة / آخرون / غير واحد` 等は「その他多数」を意味する曖昧表現。これらは relation / teacher / student / family 等に記録しません。

#### 6. 史料の参照マーカー

`الآتي`(後述する人)や `الماضي / المتقدم`(前述した人)は、別の伝記エントリへの相互参照を示します。これらを見つけた場合は editors_notes に "Marked الآتي — cross-ref to a later entry." 等のメモを残します。

#### 7. 生地・没地・埋葬地(v20.1 以降)

- `ولد بـ X` / `مولده بـ X` → birth_place_*
- `مات بـ X` / `توفي بـ X` / `المتوفى بها` → death_place_*
- `دفن بـ X` / `دفن بتربة X` → burial_place_*
- 葬儀の祈り(`صلى عليه`)は death_note に統合する(独立 event にしない)

#### 8. 描写的記述は bio_events に記録しない(v20.2 以降)

「学者から学んだ」「美しく朗誦した」「商人として有名だった」のような **描写的・包括的な記述** は bio_events に記録しません。bio_events は **具体的な日付のあるイベント** のみ。

---

## 既知の問題と運用上の注意

### AIND-D ID 欄が空のまま

- 史料テキストに `###$AIND-DXXXXX | NNNNNNNNNNNN$#` ヘッダーが含まれていない可能性
- 解析後に手動で AIND-D ID と 12 digits ID 欄に入力できます
- ヘッダー形式が `###$AIND-D00001 | 932540579843$#` のように `|`(パイプ)で区切られているかを確認

### 12 digits ID が 12 桁にならない

- 12 桁の半角数字でない場合、警告が表示されます
- 全角数字や空白の混入に注意

### 解析ボタンを押しても何も起きない

- Gemini API のキーが secrets に設定されているか確認
- ネットワーク接続を確認

### AI の抽出結果が明らかに間違っている

- AI は完璧ではありません。必ず人間が確認します
- 明らかな誤りは手動で修正してください
- AI が頻繁に間違う部分があれば、Geminiプロンプトの調整が必要(管理者に連絡)

### スプレッドシートへの保存でエラーが出る

- secrets に `[gcp_service_account]` が登録されているか
- スプレッドシートがそのサービスアカウントに共有されているか
- **v20.10 以降**: respStmt セクションが空の場合、保存できません(エラー表示)。respStmt で担当者を追加してください。

### スプレッドシートの C 列・D 列がアプリで更新されない

- これは **仕様** です。C 列(AIND-D ID)と D 列(12 digits ID)はアプリでは触らず、ユーザーが手動で記入する運用です
- 新規行追加時は、保存後にアプリが「手動で記入してください」と通知します

### ページを閉じたらデータが消えた

- アプリのデータはセッション中のみメモリに保持されます
- 作業終了時は必ず XML ダウンロードまたはスプレッドシート保存を行ってください

---

## 用語集

| 用語 | 意味 |
|---|---|
| **al-Sakhāwī** | 本プロジェクトの対象著者。シャムス・アッディーン・ムハンマド・アッ＝サハーウィー(d. 902 H / 1497 CE) |
| **al-Ḍawʾ al-Lāmiʿ** | 『輝く光』。サハーウィーによる 9 世紀ヒジュラの人物録 |
| **TEI** | Text Encoding Initiative。人文学のテキストを XML で構造化する国際標準 |
| **nisbah** | アラビア語の系譜的形容詞(出身地・部族・職業等を示す) |
| **nasab** | アラビア語の系譜(父・祖父・曽祖父…と遡る命名) |
| **kunyah** | アラビア語のテクノニム(`أبو فلان` = ◯◯の父) |
| **laqab** | 名誉称号(`زين الدين` 等) |
| **madhhab** | イスラム法学派(ハナフィー・マーリク・シャーフィイー・ハンバリー等) |
| **hajj / umrah** | メッカへの大巡礼 / 小巡礼 |
| **jāwara** | メッカ/メディナでの長期滞在 |
| **riḥla** | 学問的旅行(知識を求めて各地を巡る) |
| **isnād** | ハディースの伝達経路 |
| **ijaza** | 教授証明(師が弟子に与える、教えることを許可する書状) |
| **IJMES** | International Journal of Middle East Studies の翻字規則 |
| **turba**(تربة) | 墓所・霊廟 |
| **al-Muʿallā**(المعلاة) | マッカの主要墓地 |

---

# 変更履歴(主要バージョン)

## v20.10(2026-05-13)— 担当者入力の一本化 + DEBUG 削除

**主要変更**:
- **担当者入力の重複を解消**: スプレッドシート保存セクションの「担当者」セレクトボックスを削除。**respStmt セクションの最初のエントリの作業者名** が自動的にスプレッドシート B列に使われる
- respStmt が空でスプレッドシート保存を押すとエラー表示
- v20.5-debug2 で追加していたデバッグ枠を完全削除(問題解決のため)

## v20.9(2026-05-13)— 孤児 ID の自動防止

**問題**: テキスト本文に書物言及がないのに、teachers の `text_id` だけ番号が振られていた(例: `TMP-T-00031`)。

**解決(三重防御)**:
1. JSON テンプレートのプレースホルダー(`TMP-T-00000` 等)をすべて空欄に変更
2. Gemini プロンプトに「**名前が空欄なら ID も空欄**」を冒頭で明示。15 個の (name, id) ペアを列挙
3. 採番ロジックに「ペア名前空欄ならプレースホルダー ID をクリア」を追加

## v20.8(2026-05-13)— Madhhab 複数対応

転向した人物(例: シャーフィイー派 → ハンバリー派)を時系列順に記録できるよう、Madhhab を配列化:
- データ構造: `madhhab`(単一) → `madhhabs`(配列)
- UI: 順番セレクト(1〜3)+ 削除 + 追加ボタン(最大 3 つ)
- XML 出力: 複数時は `<affiliation type="madhhab" n="1" ref="...">` のように `n` 属性付き
- スプレッドシート I列: 複数なら `Shafi'i → Hanbali` と矢印連結
- 旧データの自動マイグレーション(`madhhab` → `madhhabs[0]`)

## v20.6 / v20.7(2026-05-13)— Streamlit session_state 同期問題の解決

**問題**: Gemini が正しい翻字(`Makka` / `al-Muʿallā`)を返しているのに、UI には古い値(座標)が表示される。

**原因**: Streamlit text_input の `key` 指定時、session_state が「正」として優先される仕様。過去セッションの値が UI に居座る。

**解決**: `apply_prompt_result` で 9 つの place 用 UI キー(`birth_place_ar_input` 等)を解析実行のたびに data の値で直接同期(`del` ではなく `=` で代入)。

## v20.5(2026-05-13)— プロンプト強化 + XML バージョン記録 + UI 微修正

- Gemini プロンプトの冒頭に「TOP-PRIORITY RULES」セクションを追加(`_lat` 欄に座標を入れない等)
- XML 出力に生成元バージョンをコメントとして埋め込む: `<!-- generated by AINet-DB Researcher Pro v20.5 (2026-05-13) at ... -->`
- Madhhab UI のラベル位置ズレ修正

## v20.4(2026-05-13)— 翻字欄の不正値後処理

Gemini が `_lat` 欄に座標数値(`21.426667` 等)を入れる現象に対処。プロンプト指示に加え、後処理で座標形式の値を自動的にクリアする保険コードを追加。

## v20.3(2026-05-13)— UI key 衝突修正

9 つの place フィールド(`birth_place_*` / `death_place_*` / `burial_place_*`)の UI key を `_input` サフィックス付き(`death_place_ar_input` 等)に変更。データキーと衝突しないように。

## v20.2(2026-05-13)— Gemini プロンプト大幅強化

- Birth/Death/Burial セクションを「CRITICAL」化、アラビア語キーワードと期待出力をマッピング
- 葬儀祈り(`صلى عليه`)を `<death>` の note に統合する旨を明示(独立 event にしない)
- bio_events の過剰生成防止(描写的記述は bio_event ではない、と例示)
- プロンプト末尾にチェックリスト追加

## v20.1(2026-05-13)— Birth/Death に placeName 統合

- 9 つの新フィールド: `birth/death/burial_place_ar/lat/id`
- `<birth>` / `<death>` 内に `<placeName>` を追加。埋葬地は `type="burial"` で `<death>` 内に統合
- Activities から `buried` タイプを削除(廃止)
- 旧データの自動マイグレーション(`buried` activity → `burial_place_*`)

## v20.0(2026-05-11)— AIND-D ベースに復帰

- xml:id を `id_{12桁}` から `AIND-DXXXXX`(5 桁、テキストヘッダーから)に変更
- 12 桁数字は `source` 属性として保持
- 進捗ラベル機能を完全廃止(スプレッドシート C 列はユーザー手動記入用に)
- 入力テキストの形式を `###$AIND-DXXXXX | NNNNNNNNNNNN$#` に
- UI に「AIND-D ID」と「12 digits ID」の 2 入力欄

## v19.x(2026-05-11)以前

- 進捗ラベル機能、xml:id を 12 桁ベースに統一する試み(後に v20.0 で AIND-D ベースに戻る)
- AIND-D 連番重複・物理順序不整合・AIND-D 未付与立項などのデータ品質改善

## v2.x(2026-05-02 ～)

- 翻字一括補完ボタン、自動採番ロジック、ID Master 照合機能の導入
- Biographical Events、Social Relations の新規セクション
- Sex フィールド、Cert/Note、Hajj/Umrah、Method/Field 分離

## v1.0(初版)

『輝く光』の人物伝記から構造化データを抽出する基本機能を実装。
