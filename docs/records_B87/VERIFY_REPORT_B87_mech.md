# B87 機械検査パス報告 — VERIFY_REPORT_B87_mech

- 検査者: `claude-b87-verify-mech`
- 実施日: 2026-09-18
- 対象: `docs/_work/B87_incoming/B87/` 全 77 ファイル（実項 69 / REF 8）
- 参照: `docs/records_B87/CHUNK_BRIEF_B87.md`（統一規則1〜19）、`docs/_work/idmaster_working_20260918.tsv`（7,678 ID）、`docs/_work/B87_incoming/corpus_B87.json`、`0__DawForAIND_renumbered_B54_split20260905.txt`、`docs/records_B87/TMP_NEW_B87a〜d.tsv`、`Individuals/`（2,359件・うち REF 152件）
- 検査スクリプト: `docs/_work/B87_incoming/verify/`（`chk.py`, `r1.py`〜`r17.py`）。**全項目を機械実行**（目視判定なし）。
- 本報告は**構造・規約・ID 整合の機械的検査のみ**。内容の事実検証は対象外。

---

## 総括

| | 件数 |
|---|---|
| 検査項目 | 13（＋派生サブ検査 9） |
| 違反総数 | **2** |
| 自動修正 | **2** |
| 残件（要裁定） | **0** |

---

## 検査項目別

### 1. well-formed

| 項目 | 値 |
|---|---|
| 検査対象 | 77 |
| 違反 | **0件** |
| 自動修正 | 0 |
| 残件 | 0 |

`xmllint --noout` 全 77 件 pass。ElementTree でも全件パース成功。
付随確認（いずれも **0件**）:
- ファイル名 ↔ ルート `@xml:id` の一致 … 0件不一致
- ファイル名の 12 桁 ↔ ルート `@source` の一致 … 0件不一致
- `<!-- generated by AINet-DB Researcher Pro v20.11.1 -->` の存在 … 77/77 存在

### 2. `relation/@n` の規約A適合（統一規則19）

| 項目 | 値 |
|---|---|
| 検査対象 | 77ファイル / relation 179件（親族 106 / 非親族 73） |
| 親族 subtype に `@n` が付いている | **0件** |
| 非親族の連番が 1 起点の通し番号でない（欠番・重複・subtype ごと振り直し） | **0件** |
| 自動修正 | 0 |
| 残件 | 0 |

親族判定に用いた subtype: father / mother / son / daughter / brother / sister / grandfather / grandson / grandmother / uncle / aunt / cousin / nephew / niece / spouse / relative / ancestor / child / siblings_child / maternal_uncle / maternal_grandfather / paternal_grandfather / granduncle / `*-in-law`（本バッチ実測では father-in-law・brother-in-law）。
非親族として連番対象になった subtype: teacher / student / patron / successor / predecessor / rival / met / possible_identity / other / killer / companion / colleague。
→ **4チャンクとも規約A に統一済み。旧規約（全 relation 通し番号）の残存はゼロ。**

### 3. 原文 `<note type="source" xml:lang="ar">`

| 項目 | 値 |
|---|---|
| 存在（ちょうど1個） | 77/77（REF 8件も全件保持）。**欠落 0件** |
| `<note type="translation">` の直前に配置 | 違反 **1件** → **自動修正済み** |
| corpus 本文との一致（空白・ハラカ・タトウィール正規化後） | **不一致 0件** |
| 本文以外の混入（他項本文 / `###$` / `$$$` / 刊本立項連番） | **0件** |
| 自動修正 | 1 |
| 残件 | 0 |

**配置違反（自動修正）**
- `REF_AIND-D07344_329023418804.xml`: 配列が `source → reference → translation(ja) → translation(en)` となっており、`note type="source"` が `note type="translation"` の直前でなかった。他の REF 7件および既存 `Individuals/REF_*.xml`（152件）と同じ `source → translation(ja) → translation(en) → reference` に並べ替え。内容は無変更。`<respStmt>`（resp=検証 / `claude-b87-verify-mech` / 2026-09-18）を追加。

**corpus 一致の内訳**
- 完全一致 … 9件（`D04470` `D04503` `D07323` `D07325` `D07344` `D07679` `D07683` `D07684` `D07686` ＝ corpus 側に刊本立項連番が無い9項）
- 先頭の刊本立項連番のみが除かれている … 68件（＝**正しい処理**。ブリーフの「立項連番を残さない」に適合）
- それ以外の差分 … **0件**

**corpus_B87.json 自体の健全性検証**（派生検査）
77件すべてについて corpus 原本から本文を再抽出し（`###$` 開始行 〜 次の `###$` 直前、`|` 始まり行を除外）JSON と照合。差分は区切り記号 `$` 1文字のみで、**本文の取りこぼし・混入はゼロ**。

### 4. respStmt

| 項目 | 値 |
|---|---|
| 検査対象 | 77（respStmt 計 154個） |
| 初版 respStmt の削除 | **0件** |
| 初版 respStmt の改変 | **0件** |
| 校閲者 respStmt の欠落 | **0件** |
| 自動修正 | 0（項目3の1件に検証 respStmt を追加したのみ） |
| 残件 | 0 |

- 全 77 ファイルが `respStmt` を 2個（D07344 のみ本検査により 3個）保持。
- 第1 respStmt は 77/77 とも構造が完全同一 `(resp, persName, date)`、`resp`=「初版作成」。
  - persName 内訳: `Assistant 4` 57件 / `Naoki Umetsu` 20件（いずれも repo 既存 2,359件の初版作成者名簿に載る正規の値）
  - date 内訳: 2026-09-05(39) / 2026-08-22(18) / 2026-09-13(11) / 2026-09-12(7) / 2026-08-31(2)
  - **生成コメントのタイムスタンプ（UTC）との突合**: 11件（`D07681` `D07682` `D07685` `D07686` `D07688` `D07689` `D07691` `D07692` `D07695` `D07696` `REF_D07684`）で「コメント 2026-09-12T22:44〜23:36Z / respStmt 2026-09-13」の1日ずれ。**JST 換算による正常なずれ**で改変ではない。他 66件は完全一致。
- 第2 respStmt（校閲）は 77/77 とも `date when="2026-09-18"`、persName は `claude-b87a-review`(20) / `claude-b87b-review`(19) / `claude-b87c-review`(19) / `claude-b87d-review`(19)。**チャンク割当と完全一致**。

### 5. ID 形式

| 検査 | 違反 |
|---|---|
| `AIND-D` = 5桁 | **0件** |
| `TMP-P` = 6桁（または仮番号 `NEW87x-nn`） | **0件** |
| `TMP-N/L/I/O/T/S` = 5桁（または仮番号） | **0件** |
| TMP の `#` 接頭辞 | **0件**（属性値 316件すべて `#TMP-…`） |
| Wikidata の `wd:` 接頭辞 | **0件**（91件すべて `wd:Q…`。裸の `Q…` ゼロ） |
| GeoNames の `gn:` 接頭辞 | **0件**（73件すべて `gn:…`。裸の数値 ID ゼロ） |
| `xml:lang="lat"` の使用 | **0件**（`ar-Latn` に統一済み） |
| `<desc>` の `xml:lang` 欠落 | **0件** |
| 上記以外の異常な参照値 | **0件**（`#NEEDID` 24件は規則12により正当） |
| 自動修正 | 0 |
| 残件 | 0 |

※ 注記：`ref=""`（空値）・`TMP-…`（`#` 無し）の見かけ上の検出は、いずれも **`<note>` 本文中で校閲者が初版の誤りを引用して説明している散文**であり、実際の属性値ではない（ElementTree による属性値限定の再検査で 0件を確認）。

### 6. 参照 ID の実在

| 項目 | 値 |
|---|---|
| `#AIND-Dxxxxx` 参照（distinct） | 161 |
| うち幻番号（`Individuals/` にも corpus にも本バッチにも無い） | **0件** |
| `#TMP-*` 参照（distinct、仮番号を除く） | 136 |
| うち ID-Master 未登録＝幻番号 | **0件** |
| 自動修正 | 0 |
| 残件 | 0 |

- `#AIND-D` 161件の内訳: `Individuals/` に XML 実在 10件 ／ 本バッチ内 69件 ／ corpus のみ 82件（合計 161、**どこにも無いもの 0件**）。corpus のみのものは**未XML化の既存立項**であり幻番号ではない（corpus 総数 13,911項 に対し `Individuals/` は 2,359件）。
- precheck が挙げた幻番号 `TMP-P-000131`（D07341）は解消済み（下記8参照）。
- 仮番号 `TMP-*-NEW87[a-d]-nn` 52件は検査対象外（統合時に一括採番）。

### 7. 毒wd の残存

| wd | 属性値としての残存 |
|---|---|
| Q160851（→ Q228986 Hanafi） | **0件** |
| Q191314（→ Q233387 Hanbali） | **0件** |
| Q3824443 | **0件** |
| Q35160（→ Q428858） | **0件** |
| Q12247942（→ Q108806953） | **0件** |
| Q381208（→ `#AIND-D02168`） | **0件** |
| 自動修正 | 0（すでに全件是正済み） |
| 残件 | 0 |

- 現在の `affiliation[@type="madhhab"]` 実測: Shafiʿi `wd:Q82245` 9件 / Maliki `wd:Q48221` 9件 / Hanafi `wd:Q228986` 5件 / Hanbali `wd:Q233387` 1件 — **全件が正しい学派 ID**。
- `D07382` の patron は `wd:Q381208` → `#AIND-D02168` に差し替え済み（2箇所）。
- 文字列としての `Q160851`（5ファイル各1回）・`Q381208`（1ファイル2回）は、**校閲 respStmt の note 内で「毒wd Q160851 → wd:Q228986」と修正内容を記録している散文**であり、参照としては使われていない（＝正常）。

### 8. 汚染番号の残存

| 番号 | 属性値としての残存 | 判定 |
|---|---|---|
| TMP-P-000131 | 0件 | 解消（D07341） |
| TMP-P-000529 | 0件 | 解消（D07348 / D07357×2 / D07692） |
| TMP-P-000490 | 0件 | 解消（D04481 / D07374） |
| TMP-P-000530 | 0件 | 解消（D07375 / D07381 / D07677） |
| TMP-P-000204 | 0件 | 解消（D07342 / D07359） |
| TMP-P-000275 | **1件**（D07350 `relation/@active`） | **維持が REVIEW_NOTES に明記済み → 正当** |
| TMP-P-000664 | 0件 | 解消（D07677） |
| TMP-O-00096 | **1件**（D04484 `state/@ref`） | **維持が REVIEW_NOTES に明記済み → 正当** |
| TMP-O-00012 | 0件 | 解消（D07339） |
| TMP-N-00485 | 0件 | 解消（D04490） |
| TMP-N-00128 | 0件 | 解消（D07324） |
| 自動修正 | 0 | |
| 残件（要裁定） | **0件** | |

**維持された2件の根拠照合（いずれも記載あり＝報告不要）**
- `D07350` / `#TMP-P-000275`（الحجار）: `REVIEW_NOTES_B87c.md` 45行目・112行目に「登録実体（الحجار）と一致＝維持」、XML 内 note にも「本項では登録実体そのものが該当するため維持」と明記。
- `D04484` / `#TMP-O-00096`（نائب الحكم）: `REVIEW_NOTES_B87a.md` 64行目に「登録実体（نائب الحكم）と一致＝正用のまま」、XML 内 note にも「#TMP-O-00096＝登録実体と一致、precheck 汚染注意は本項では正用」と明記。

※ 他の 16 ファイルに現れる汚染番号の文字列は、すべて**校閲 note 内の「初版の #TMP-P-000xxx を〜に差し替えた」という修正記録の散文**である。

### 9. ★仮TMP のチャンク間重複／ID-Master 既存登録

| 項目 | 値 |
|---|---|
| 検査対象 | TMP_NEW_B87a〜d 計 52行 |
| TSV 内の仮ID 重複 | **0件** |
| チャンク間の実体重複（同一人物・同一地名に別番号） | **0件** |
| ID-Master にすでに登録されている実体 | **0件** |
| 自動修正 | 1（TSV 列数、下記参照） |
| 残件 | 0 |

**重複突合の方法**（機械実行）
Arabic はハラカ・タトウィール除去、`أ/إ/آ/ٱ→ا`、`ة→ه`、`ى→ي` 正規化＋**定冠詞 `ال` の有無**の両パターン、Latin は分音記号除去＋`al/b./ibn/bin/abū/abī` 除去で正規化し、同カテゴリ内の全ペアを `difflib` 比較（閾値 0.62／部分包含も検出）。
- 抽出された候補ペア 15組をすべて確認 → **全組がイスム（第一名）の異なる別人／別地名**であり真の重複はゼロ。
  - クロスチャンク候補（8組）は「同じ父名・同じニスバ要素を共有するだけ」の類似（例 `NEW87a-05 عبد الملك بن إبراهيم الدميري` × `NEW87c-01 أحمد بن إبراهيم العسيلي`、`NEW87a-01 الجانبكية` × `NEW87c-01 الجردكية`）。
  - 最高スコア 0.91 は同一チャンク内の `NEW87c-09 أحمد بن محمد بن داود البدراني` × `NEW87c-10 علي بن محمد بن داود البدراني` ＝ **兄弟**（別人・同チャンク発行なので重複発行ではない）。

**ID-Master 再 grep**（52件 × 別名分割、定冠詞有無・`ة/ه` 揺れを含む 12,747 正規化キーに対して照合）
- 完全一致ヒット … **0件**
- 同カテゴリ内ファジー一致（ratio ≥ 0.80）… 該当 6件あるが**いずれも別実体**（`بين القصرين` vs `بين السورين` 0.82、`المرقب` vs `المغرب` 0.83、`الدقاق` vs `الدقماقي` 0.86、`الجردكية` vs `البردبكية` 0.82、他）
- 索引の健全性は既知 ID（عبد الله→TMP-P-000490、القاهرة→360630、الحجار→TMP-P-000275、نائب الحكم→TMP-O-00096、المسلسل→TMP-T-00019）で自己検証済み。
→ **52件すべて新規発行が妥当。**

**自動修正（TSV 形式）**
- `TMP_NEW_B87c.tsv` の 16行すべてが 6列（末尾の空列欠落）。ブリーフ指定の 7列（空, category, Arabic, Latin, 仮ID, note, 空）に合わせ末尾空列を補填。a/b/d は元から 7列。
- カテゴリ表記は ID-Master の語彙（Person / Nisbah / Location / Institution / Office / Text / Subject）と 52件すべて整合。

### 10. 仮TMP の参照整合（TSV ↔ XML）

| 項目 | 値 |
|---|---|
| TSV 記載の仮番号 | 52 |
| XML の属性値に現れる仮番号（distinct） | 52 |
| TSV にあるが XML で未使用（片側漏れ） | **0件** |
| XML にあるが TSV 未記載（片側漏れ） | **0件** |
| 自動修正 | 0 |
| 残件 | 0 |

チャンク別内訳: a=12 / b=14 / c=16 / d=10。カテゴリ別: P=30 / O=7 / T=5 / L=3 / I=3 / N=1。仮番号の連番も各チャンク内で 01 起点の欠番なし。

### 11. REF 8件の形式

| 検査 | 違反 |
|---|---|
| `type="reference" subtype="LIMBO"` | **0件**（8/8 適合） |
| 実伝由来要素（`state`/`event`/`birth`/`death`/`affiliation`）の残存 | **0件** |
| `<note type="reference" target="#AIND-Dxxxxx">` の存在 | **0件**（8/8 保持、`#NEEDID` ターゲットは 0件＝全件で転送先を特定済み） |
| ファイル名の `REF_` 接頭辞 | **0件** |
| 既存 `Individuals/REF_*.xml` との形式一致 | **1件** → 自動修正済み（項目3） |
| REF 以外のファイルが誤って `type="reference"` | **0件** |
| 自動修正 | 1 |
| 残件 | 0 |

- 既存 REF 152件はルート属性が全件 `type="reference" subtype="LIMBO"`、ルート直下の要素集合が `{persName, sex, note, respStmt}`。**B87 の REF 8件も 8/8 が同じ要素集合**（実伝由来要素ゼロ）。
- 転送先 ID の実在確認: `#AIND-D04465` `#AIND-D04514` `#AIND-D08818` `#AIND-D07851` `#AIND-D03619` `#AIND-D10951` `#AIND-D07668` `#AIND-D06693` — **8件すべて corpus に実在**（項目6で幻番号ゼロを確認済み）。
- REF 8件の内訳はブリーフの「REF 有力候補」8件と完全一致（`D04470` `D04503` `D07323` `D07325` `D07344` `D07679` `D07683` `D07684`）。ブリーフが「実項」と指定した `D07686` は実項のまま（`REF_` 接頭辞なし・`type` 属性なし）＝正しい。

### 12. affiliation と state の二重符号化（統一規則8）

| 項目 | 値 |
|---|---|
| 検査対象 | 77（affiliation 25件 / state 33件） |
| 同一 `@ref` を affiliation と state の双方に持つファイル | **0件** |
| 自動修正 | 0 |
| 残件 | 0 |

affiliation の実測は `type="madhhab"` 24件 ＋ `type="reside"` 1件（`#TMP-I-00059`）のみで、施設・職名の二重符号化は存在しない。

### 13. ヒジュラ暦の `@when` 混入

| 項目 | 値 |
|---|---|
| 検査対象 | 日付属性 230個（`@when` / `@notBefore` / `@notAfter` / `@from` / `@to`、全要素） |
| 年が 0600〜0999 の4桁＝ヒジュラ年誤置の疑い | **0件** |
| 自動修正 | 0 |
| 残件 | 0 |

`@when-custom`（ヒジュラ）は 77個すべてが規則18どおりの4桁ゼロ詰め（`0xxx` 形式、月日付きも含め逸脱 0件）。西暦側とヒジュラ側の取り違えは検出されず。

---

## 派生検査（機械実行・参考）

| 検査 | 結果 |
|---|---|
| 訳文の言語構成（ja/en 各1個） | 77/77 適合、**違反 0件** |
| en 訳への日本語（CJK）混入 | **0件** |
| 訳文への内部ID（`#AIND-` / `TMP-` / `#NEEDID`）漏れ | **0件** |
| 訳文冒頭への刊本立項連番混入（例「107番。」） | **0件**（precheck 指摘の `D07692` も解消済み） |
| ja 訳の語中 `بن`＝「ビン」表記（規則17） | **0件**（検出1件は `بنت`＝「ビント」で規則対象外） |
| ルート直下 `event` / `state` の `@n` | 全 79 event・33 state に `@n` あり（欠落 0）。6ファイルで type ごとの振り直し（例 `residence n=1` ＋ `cultural n=1`）があるが、これは **repo 既存 1,253ファイル中 211ファイルと同じ従来慣行**であり規則19（relation 限定）の対象外 → **違反としない** |
| `<note>` の `@type` 語彙 | `source`/`translation`/`reference`/`personalia`/`editorial`/無指定 — いずれも repo 既存語彙内、**逸脱 0件** |

---

## 自動修正の一覧（2件）

| # | 対象 | 内容 | respStmt |
|---|---|---|---|
| 1 | `docs/_work/B87_incoming/B87/REF_AIND-D07344_329023418804.xml` | `<note type="reference">` を translation の後ろへ移動し、`source → translation(ja) → translation(en) → reference` に統一（他 REF 7件・既存 REF 152件と同配列）。テキスト内容は無変更 | resp=「検証」/ `claude-b87-verify-mech` / 2026-09-18 を追加 |
| 2 | `docs/records_B87/TMP_NEW_B87c.tsv` | 全 16 行が 6列だったため末尾の空列を補填し 7列に統一 | （TSV のため respStmt なし） |

修正後、77 ファイル全件で `xmllint --noout` を再実行し **well-formed を再確認**。項目2・3の再検査も **違反 0件** で完走。

## 残件（要裁定）

**0件。** 判断を要する未解決事項は検出されなかった。

（本パスは構造・規約・ID 整合のみを対象とする。原文解釈・同定の妥当性・訳文の正確さ・`[要照合wd]` の実照合は、内容検証パスおよび Waka 裁定の対象。）
