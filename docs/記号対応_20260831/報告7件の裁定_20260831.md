# アルバイトからの報告7件 — 裁定（2026-08-31 / claude-opus-5）

**結論から: 7件すべて作業を止める必要はありませんでした。** うち5件は完全に無視してよい記号、2件は該当箇所だけ空にすれば作業続行できます。

---

## ◎ そのまま作業してよい（5件）— 記号は `±±±`

3名の報告にあった「＋記号」は、実際には **`±±±`** という別の記号です。
コーパス全体で150箇所ありますが、**150箇所すべてで完全な単語の直後に付いており、語を途中で切ることは一度もありません**。取り除いた語がそのまま正しい読みです。

| ID | 該当語 | 記号を外した読み | 判定 |
|---|---|---|---|
| **D04460** | `الغزي±±±` | `الغزي`（ガザの人・ニスバ） | ◎ そのまま作業可 |
| **D04475** | `الحجار±±±` | `الحجار`（＝著名なムスニド。「كلهم عن الحجار سماعا」で文意も通る） | ◎ そのまま作業可 |
| **D04636** | `الطنبدية±±±` / `للطنبذية±±±` | `الطنبدية` / `للطنبذية` | ◎ そのまま作業可（下記※） |
| **D04555** | `الحراري±±±` | `الحراري`（ニスバ） | ◎ そのまま作業可 |

※ **D04636 だけは1点だけ実質的な情報があります**。同一項の中で同じマドラサ名が **`الطنبدية`（د）と `للطنبذية`（ذ）の2通り**に綴られています。これは記号の問題ではなく**原文の異綴**なので、TMP-I を起こす際に「د/ذ の異綴あり」と注記しておくと後で名寄せが効きます。ただし作業を止める理由にはなりません。

---

## ○ 該当箇所だけ空にして作業続行（2件）

### D04472 — 記号は `...`（原文の欠落）

```
عبد الله بن عبد الرحيم الحضرمي ابن أخت عبد الكبير . مات بمكة في صفر سنة ...
```

`...` は **原文に没年が書かれていない**という意味です。「3点リーダーが入っているから作業しない」ではなく、
- 没地 = مكة（`gn:104515`）
- 没月 = صفر
- **没年 = 記載なし**（`<death>` に年を入れない）

として作業を続けてください。**項全体を飛ばす必要はありません。**

### D04827 — 記号は `...` と `=380 above?`（欠落＋人間のメモ）

```
عبيد ويدعى عبد الغني بن كاتب الجيش الفخر بن الجيعان . كذا رأيته بخط الفخر بن ...
فيمن سمع من شيخنا في أماليه القديمة وأظنه وهم في قوله ويدعى بل هو عبد الوهاب
بن الفخر بن عبد الغني =380 above? .
```

梅津さんは「文末が不自然」と判断されましたが、**不自然なのは文末に人が書き足した `=380 above?` というメモの部分だけ**で、サハーウィーの文章自体は完結しています。

この項は**サハーウィー自身の訂正メモ**です:
> 「ʿUbayd、またの名を ʿAbd al-Ghanī、軍務書記官 al-Fakhr b. al-Jīʿān の子。**そのように al-Fakhr b. …（欠落）の筆跡で見た**、我らがシャイフの旧『アマーリー』の聴講者一覧の中に。**だが「またの名を」というのは彼の思い違いだと私は思う。実際は ʿAbd al-Wahhāb b. al-Fakhr b. ʿAbd al-Ghanī である。**」

対応:
- `كذا رأيته` = サハーウィー自身の目撃 → **Pattern A では非採用**（標準ルールどおり）
- `بخط الفخر بن ...` = 筆跡の主の名が欠落 → `#NEEDID` のまま
- `وأظنه وهم` = サハーウィーの推測 → **cert="medium"**（要判断§D-5 で「أظن」の段階を裁定中）
- `=380 above?` = **本文ではないので無視**。ただしメモの主旨（この人物は上の380番と同一人物か？）は名寄せ課題として残ります → 欠番ウォッチリストへ

---

## ○ 疑義語だけ保留して作業続行（1件）

### D04983 — 記号は `★語?★`（準備担当者の読み疑義）

```
علي بن أحمد بن عبد الله بن محمد بن إبراهيم بن نصر بن فهد ★الديراسطياري?★
سمع في سنة سبع وستين من الصلاح بن أبي عمر وجوزت إدراكه لهذا القرن .
```

`★…?★` は**準備担当者（人間）が「この語の読みが確定できない」と付けた印**で、サハーウィーの本文ではありません。

`الديراسطياري` はコーパス全13,821項で**この1箇所にしか出ない語**（ハパックス）なので、コーパス内からは読みを確定できません。ですが、**それ以外の情報はすべて健全**です:
- 名前 `علي بن أحمد بن عبد الله بن محمد بن إبراهيم بن نصر بن فهد` → そのまま
- 師 `الصلاح بن أبي عمر` に 767年に聴聞 → teacher relation（active=師）
- `وجوزت إدراكه لهذا القرن` = サハーウィーが「9世紀に入っていると判断した」 → cert 付きで記録

対応: **ニスバ `الديراسطياري` だけ `#NEEDID` のまま**にして、他は通常どおり作業してください。

---

## この7件から見えた構造的な問題

作業者が止まったのは**判断力の問題ではなく、渡しているテキストの問題**です。
現在コーパスには3つの層が混ざったまま作業者に渡っています。

1. サハーウィーの本文
2. 変換ノイズ（`±±±`・孤立`★`・全角スペース）
3. 準備担当者が書き込んだメモ（`=380 above?`・`★語?★`・`★ID_Missing★`）

**2と3は作業者の目に触れる必要がありません。** 分離する前処理を用意しました（`precheck_corpus_anomalies.py` と `corpus_clean_for_workers.txt`）。
