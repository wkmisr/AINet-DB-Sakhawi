# Waka 裁定反映記録 — B87（2026-09-21）

- 担当: `claude-b87-apply3`
- ブランチ: `B87-20260918`（git 操作なし。`git diff` による確認のみ）
- 対象: `Individuals/` 内の B87 該当 77 ファイル（実項 69 ＋ REF 8）、`docs/records_B87/TMP登録_B87.tsv`
- 典拠: `docs/要判断_B87_残件.md` §1（★1〜★5）、`docs/records_B87/CHUNK_BRIEF_B87.md`
- corpus: `0__DawForAIND_renumbered_B54_split20260905.txt`
- 換算: `python3 docs/records_B79-83/tools/h2g.py`（`--selftest` PASS を確認のうえ使用）

> **編集先について**: `docs/_work/` は `.gitignore` 対象の作業コピーであり、git 追跡対象は `Individuals/` のみ。2026-09-19 の裁定反映も `Individuals/` だけを更新している（77件中8件で `docs/_work/B87_incoming/B87/` と差分あり）。本作業も先例に従い `Individuals/` のみを編集した。

---

## 裁定A【§D-1】`AIND-D04484` の没年 806 を確定

### 確認した根拠（適用前の検算）

corpus 原本 49368–49370 行:

```
###$AIND-D04484 | 985703392983$# $ 103 عبد الله بن عبد الله الدكاري المغربي ثم
المدني المالكي . أقرأ بها ودرس وأفاد وناب في الحكم في بعض القضايا
وكان يتجرأ على العلماء . مات في سنة ست سامحه الله . قاله شيخنا في إنبائه .
```

- 原文は `مات في سنة ست`（一の位のみ）。Waka 裁定「806年ですので、cert high でいいですよ」により 806 で確定。**前提と原文は一致**。
- 西暦の再計算: `h2g.py 806 1 1` → `806 -> 1403-07-30 .. 1404-07-18 (year end)`。既存の `when="1403"`（年精度＝年初）は正しく、**変更不要**。

### cert の表現形 — repo 実測により「cert 属性を外す」形を採用

| 対象 | 件数 |
|---|---|
| `<death>` 要素 総数 | 1,255 |
| うち `cert` 無記載 | **1,137（90.6%）** |
| うち `cert="high"` | 17 |
| うち `cert="medium"` | 60 |
| うち `cert="low"` | 41 |

（参考: repo 全体の `cert="high"` は 278 箇所・236 ファイル。）

→ **repo の多数派慣行は「cert 無記載＝確実」**。したがって `cert="high"` を明記せず `cert` 属性を除去する形で裁定を反映した。この判断根拠は当該ファイルの `<death>` note と `<respStmt>` note に明記してある。

### 変更箇所 — `Individuals/AIND-D04484_985703392983.xml`

1. `<death when-custom="0806" when="1403" cert="low">` → `<death when-custom="0806" when="1403">`（`cert="low"` 除去）
2. `<death>` 内 ja note: 「『إنباء الغمر』の該当年で確認が要る／806・816・826・836・846 の5択」という留保を**撤回**し、「2026-09-21 Waka 裁定により 806 で確定」＋ cert 無記載の理由＋ h2g.py による再計算値に書き換え
3. `<death>` 内 en note: 同内容に書き換え
4. `<note type="translation" xml:lang="en">` の留保記号 `the year six (806?)` → `the year six (806)`（ja 訳文は原文どおり「（八百）六年」のままで変更不要）
5. `<respStmt resp="Waka裁定反映">` を追加

---

## 裁定B【§E-3 → 統一規則22】`AIND-D04484` の `المدينة` を除去

### 確認した根拠（適用前の検算）

原文（上掲）は `عبد الله بن عبد الله الدكاري المغربي ثم المدني المالكي . أقرأ بها ودرس وأفاد`。

- `بها` の先行詞候補は**ニスバ `المدني` のみ**。本文中に `المدينة` を含む地名の明示は**一切ない**。**前提と原文は一致**。

### 変更箇所 — `Individuals/AIND-D04484_985703392983.xml`

- `<event type="residence" n="1">` から `<placeName ref="gn:109223">المدينة</placeName>` を除去（event 自体と原文句 desc は保持）
- note を「規則22（2026-09-21 Waka 裁定）により除去。`بها` の先行詞がニスバ `المدني` のみで本文中に地名の明示がないため」に書き換え、同定上の根拠（ニスバ由来でマディーナと解される旨）は note 内に記述して placeName には戻さないことを明記

### B87 全77ファイルの同型点検

`<note type="source">`（＝原文全文）に対し、全 `placeName` のアラビア語表記を正規化照合（دياクリティカル除去・همزة/ألف・ى/ي・ة/ه を同値化）して「**原文に現れない placeName**」を機械抽出した。

- アラビア語 `placeName` 総数: **88**
- 原文に現れないもの: **適用前 7 件／7 ファイル**（検証パス報告の「9ファイル11件」は `xml:lang="ar-Latn"` の翻字重複を含む計数と思われる。アラビア語表記のみに限ると 7 件）

| ID | 位置 | placeName | 類型 | 処置 |
|---|---|---|---|---|
| D04484 | `event type="residence"` | المدينة | (a) `بها` 照応・先行詞はニスバ `المدني` のみ | **除去（本裁定の適用）** |
| D04498 | `death` | مكة | (a) `بها` 照応 | **保留**（下記 ⚠ 参照） |
| D07321 | relation 内 `event type="learning"` | مكة | (a) `بها` 照応 | **保留**（下記 ⚠ 参照） |
| D07332 | `event subtype="hajj"` | مكة | (b) حج からの内在的推定 | 維持（裁定C＝統一規則20） |
| D07341 | `event subtype="hajj"` | مكة | (b) حج からの内在的推定 | 維持（裁定C＝統一規則20） |
| D07691 | `event subtype="hajj"` | مكة | (b) حج からの内在的推定 | 維持（裁定C＝統一規則20） |
| D07342 | `event type="residence"` | الحسينية | (c) 原文句の正規化（`الحسيني سكنا` → `الحسينية`） | 現状維持・要確認として報告（下記） |

※ `D07324` の hajj `مكة` は原文に `وكان معه في سنة ست وخمسين **بمكة**` と明示があるため、そもそも「原文に現れない placeName」ではない。

### ⚠ 前提との食い違い — D04498 / D07321 は**適用を止めて保留**

裁定文は「D04498「مات بها」＝マッカ、D07321「سمع مني بها」＝マッカ は、**先行詞が本文中の地名なので保持**」としているが、corpus 原本を確認したところ**両件とも本文中に地名の明示がなく、`بها` の先行詞はニスバ `المكي` のみ**であった。

**D04498**（corpus）:
```
عبد الله بن عقيل بن مبارك بن رميثة بن أبي
نمى الحسني المكي . مات بها في جمادى الأولى سنة ست وأربعين . أرخه ابن فهد .
```
→ 先行詞は `المكي`（ニスバ）。XML 内の既存 note も「「بها」＝直前のニスバ المكي が指すマッカ」と自ら記している。

**D07321**（corpus）:
```
محمد بن حاجي بن
أحمد الشمس بن خواجا شهاب الدين بن الشهاب الهرموزي الأصل المكي الحنفي .
ممن سمع مني بها في المجاورة الرابعة ...
```
→ 先行詞は `المكي`（ニスバ）。XML 内の既存 note も「「بها」＝マッカ（本項主は المكي）」と記している。

つまり両件は **D04484 と完全に同型**であり、Waka 裁定の**原則**（「先行詞がニスバのみなら不可」）を機械的に当てると**除去対象**になる。一方、裁定文は両件を名指しで**保持**としている。原則と例示が食い違うため、**指示どおり適用を止め、両件は無変更のまま保留**とした。

- **原則どおり除去する**なら: D04498 の `<death>` から `مكة`（アラビア語・ラテン翻字の2要素）、D07321 の relation 内 `<event type="learning">` から `مكة` を除去（相当 3 要素・2 ファイル）。
- **保持する**なら: 統一規則22 の第3項を「照応代名詞の先行詞が**居住・出身を示すニスバ**（`المكي`・`المدني` 等）である場合も可」に修正する必要があり、その場合 **D04484 の除去も撤回**すべきことになる。

**この2件は Waka のご確認をお願いします。**（ご指示 1 行で、どちらの向きにも即時適用できます。）

### 併せて検出した境界事例（無変更・報告のみ）

- **D07342** `<event type="residence"><placeName ref="#TMP-L-00007">الحسينية</placeName>`
  原文は `القاهري **الحسيني سكنا** الماضي أبوه`。`سكنا`（居住地として）という明示語はあるが、placeName の表記 `الحسينية` は**ニスバ `الحسيني` からの語形展開**であり、統一規則22 第1項（ニスバからの補完を禁止）に触れうる。類型は (c) 原文句の正規化にあたり、今回の裁定が除去を指示した (a) には該当しないため**無変更**としたが、規則22 の運用線として要確認。

---

## 裁定C【§E-1 → 統一規則20】حج の placeName は現状維持 ＋ 欠落1件を補填

### B87 の `hajj` event 全件点検（6件）

| ID | placeName | 処置 |
|---|---|---|
| D04481 | **なし** | → **`مكة`（gn:104515）を追加** |
| D07324 | مكة | 維持（原文に `بمكة` の明示あり） |
| D07332 | مكة | 維持 |
| D07341 | مكة | 維持 |
| D07378 | مكة | 維持 |
| D07691 | مكة | 維持 |

D04481 は 2026-09-18 の校閲（`claude-b87a-review`）が規則16を適用して `مكة` を除去していた（当該 respStmt note に「hajj event の placeName（مكة）を除去（規則16）」と明記あり）。統一規則20 により**復元**した。原文該当句は `، وحج وربما حضر عندي .`。

### `جاور` 由来 event の点検（2件）

| ID | 要素 | placeName | 判定 |
|---|---|---|---|
| D07691 | `event type="residence" subtype="jāwara"`（`وجاور`） | **なし** | ✅ 裁定どおり |
| D07321 | `subtype="jāwara"` の語は note 内のみ（event は校閲時に除去済み） | — | ✅ 問題なし（`المجاورة الرابعة` はサハーウィー自身の滞在であり本項主の event ではない） |

→ **`جاور` 由来で placeName が付いているものはゼロ。除去作業は発生せず。**
（D07381 の `وما يجاورهما` は「隣接する」の意の一般動詞で مجاورة とは別語。event 化されておらず対象外。）

### 変更箇所 — `Individuals/AIND-D04481_175565186769.xml`

1. `<event type="religious" subtype="hajj" n="3">` に `<placeName ref="gn:104515">مكة</placeName>` を追加し、復元理由の ja note を付加
2. `<respStmt resp="Waka裁定反映">` を追加

---

## 裁定D【§E-4 → 統一規則23】`AIND-D04496` に兄弟 relation を追加

### 確認した根拠（適用前の検算）

corpus 原本 124268–124269 行（`AIND-D11145`）:

```
###$AIND-D11145 | 192468244996$# $ 519 فخر الدين بن عثمان بن علي الأبشاقي أخو
عبد الله الماضي ممن سمع على قريب التسعين .
```

→ **`أخو عبد الله الماضي` の実在を確認。前提と原文は一致。**

corpus 原本 49483–49484 行（`AIND-D04496`）:

```
###$AIND-D04496 | 638222083819$# $ 115 عبد الله بن عثمان بن علي الأبشاقي
بالمعجمة الشافعي مؤدب الأبناء ويعرف بالصعيدي . ممن سمع مني قريب التسعين .
```

→ 両項のナサブ `بن عثمان بن علي الأبشاقي` が**完全一致**。`عبد الله الماضي`（既出の عبد الله）＝本項主で確実。本項の原文に兄弟への言及は**ない**（＝他項にのみある情報＝規則23 の適用場面）。

### 変更箇所 — `Individuals/AIND-D04496_638222083819.xml`

1. `<listRelation>` に追加（父 relation の直後）:
   ```xml
   <relation type="personal" subtype="brother" active="#AIND-D11145" passive="#AIND-D04496" cert="medium">
   ```
   - 統一規則15: 親族は `active=相手`・`passive=本項主` ✅
   - 統一規則19: 親族には `@n` を振らない ✅
   - `cert="medium"`（規則23）✅
   - `<desc xml:lang="ar">أخو عبد الله الماضي</desc>` ＋ 根拠 note
2. `<respStmt resp="Waka裁定反映">` を追加

### 逆側の申し送り

`#AIND-D11145` は `Individuals/` 未収録（`docs/要判断_B87_残件.md` §4 の逆側申し送りリストに記載済み）。収録時に逆向き relation（`active="#AIND-D04496"` / `passive="#AIND-D11145"`）の追記が必要。当該ファイルの note に明記した。

### `AIND-D07685` の確認（変更なし）

裁定どおり **note 留めのまま・無変更**。念のため属性化の有無を確認した結果:

| 項目 | 現状 | 判定 |
|---|---|---|
| `الهروي` | `persName` に**なし**（nisbah は `الأبهري` のみ）。`<note type="editorial">` 内にのみ記述 | ✅ 属性化されていない |
| 祖父 `سيف الدين` | `grandfather` relation は**存在しない**。父 relation の note と editorial note 内にのみ記述 | ✅ 属性化されていない |
| 来訪時期（`حين قدومه على الظاهر جقمق مع قضاة شاه رخ`） | event・state とも**なし**。editorial note 内にのみ記述 | ✅ 属性化されていない |

→ **note に戻す作業は不要**。

### `AIND-D07327` の確認（変更なし）

`<relation type="personal" subtype="son" active="#AIND-D01311" passive="#AIND-D07327" cert="medium">` を確認。
- `active=相手（息子）`・`passive=本項主` ✅（規則15）
- `@n` なし ✅（規則19）
- `cert="medium"` ✅（規則23）
→ **規則23 に適合。維持。**

---

## 裁定E【§D-8】`AIND-D07318` は本項主を著者とする — 現状維持 ＋ 根拠の note 追記

### 適用内容

XML の**符号化内容は一切変更していない**（`event type="cultural" n="2"` の `persName`・`bibl`・`cert="medium"` すべてそのまま）。裁定の根拠を note として追記したのみ。

### 変更箇所 — `Individuals/AIND-D07318_367053320966.xml`

1. `<event type="cultural" n="2">` に ja note を追加:
   > §D-8（2026-09-21 Waka 裁定）: 「وصنف لأجله」の主語は本項主。イブン・ハジャル（773–852）は848年に存命だが75歳前後の最晩年であり、来訪した使節のために新たに2つのジュズウを起こす蓋然性は低い。また「إصلاح مشيخة أبيه لابن الجزري」は父の師承録の訂正であり、家の師承を知る息子にこそ可能な仕事。構文上は「وأذن له」で主語が交替するが、サハーウィーの文体では通常の交替。cert は medium を維持。
2. `<respStmt resp="Waka裁定反映">` を追加（XML 内容は無変更である旨を明記）

### `TMP登録_B87.tsv` の Note 著者欄

- `TMP-T-00179`（`جزء في الأذكار`）: 「著者は文脈上本項主（ISSUES §Db-3 参照）。」→ **「著者は本項主（محمد بن الجنيد الكازروني＝#AIND-D07318）。2026-09-21 Waka 裁定 §D-8 により確定（イブン・ハジャル説は不採用）。」**
- `TMP-T-00180`（`إصلاح مشيخة الجنيد الكازروني لابن الجزري`）: 同上に書き換え

→ 元は「本項主」とだけ書かれ人名の明記がなく、かつ未確定扱い（ISSUES 参照）だったため、指示どおり人名を明示し確定表現に改めた。

---

## 終了時の確認

| 確認項目 | 結果 |
|---|---|
| `Individuals/*.xml` 全件パース | **2,436 件・エラー 0** |
| 変更ファイルの `relation/@n`（規約A） | D04484 `[father:@n なし]` / D04496 `[teacher:n=1, father:@n なし, brother:@n なし]` / D04481 `[teacher:n=1, father:@n なし]` / D07318 `[teacher:n=1, father/grandfather:@n なし, companion:n=2, patron:n=3]` → **全件 OK**（親族に `@n` なし・非親族は1起点連番） |
| `TMP登録_B87.tsv` | **52行すべて 7 列タブ区切り**（変更前後で不変） |
| `h2g.py --selftest` | **PASS** |
| git 操作 | **commit / checkout / stash は一切実行せず**（`git diff` / `git status` のみ） |

### 変更ファイル一覧（`git diff --stat`）

```
 Individuals/AIND-D04481_175565186769.xml   |  8 +++++++-
 Individuals/AIND-D04484_985703392983.xml   | 16 ++++++++++------
 Individuals/AIND-D04496_638222083819.xml   |  7 +++++++
 Individuals/AIND-D07318_367053320966.xml   |  7 +++++++
 docs/records_B87/TMP登録_B87.tsv            |  4 ++--
```

---

## 保留・申し送り

1. **【要裁定】D04498 / D07321 の `مكة`**（上記 裁定B の ⚠）— 裁定文の例示（保持）と原則（先行詞がニスバのみなら不可）が原文と照らして食い違うため適用を停止。除去する場合は 2 ファイル・3 要素。保持する場合は統一規則22 第3項の文言修正と D04484 の撤回が必要。
2. **【要確認】D07342 の `الحسينية`** — `الحسيني سكنا` からの語形展開。規則22 第1項（ニスバからの補完禁止）と第2項（原文明記の地名は必ず placeName）の境界。今回は無変更。
3. **【逆側申し送り】`#AIND-D11145`** — 収録時に `AIND-D04496` への逆向き brother relation を追記すること。
4. `docs/_work/B87_incoming/B87/` の作業コピーは `.gitignore` 対象のため同期していない（2026-09-19 の裁定反映も同様。現在 8 ファイル＋今回 4 ファイルで `Individuals/` と差分あり）。
