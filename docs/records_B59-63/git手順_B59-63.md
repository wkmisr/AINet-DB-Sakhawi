# B59-63 コミット手順（ターミナル）

現在の状態: ブランチ **`B54`** / リモート `https://github.com/wkmisr/AINet-DB-Sakhawi.git`
`origin/main` の先端は `1a65014 B54 (#56)` ＝ **B54はマージ済み**なので、main から分岐します。

---

## ⚠ 最初に：ロックファイルの削除（私が作ってしまいました）

私が状態確認で `git status` を実行した際、マウント経由のため `.git/index.lock` を削除できませんでした。
**このままだと全ての git 操作が失敗します。**

```bash
cd ~/Desktop/AINet-DB-Sakhawi
rm -f .git/index.lock
```

---

## 1. ゴミの掃除と .gitignore の追記

`.fuse_hidden*` が **42件**、`.DS_Store` が **2件**あります（`.gitignore` 済みなので `git status` には出ませんが、掃除しておきます）。

```bash
find . -name '.fuse_hidden*' -not -path './.git/*' -delete
find . -name '.DS_Store' -not -path './.git/*' -delete
```

作業用バックアップと ID-Master の生データはコミット対象外にします（過去バッチでも一度も入れていません）。

```bash
cat >> .gitignore <<'EOF'
docs/_backup_*/
docs/_work/
EOF
```

## 2. ブランチ作成

```bash
git fetch origin
git checkout main && git pull
git checkout -b B59
```

> 作業ツリーの変更はそのまま持ち越されます。`git checkout main` でコンフリクトが出た場合は、その旨をお知らせください（B54ブランチ上から `git checkout -b B59` に切り替えます）。

## 3. ステージング（明示指定・`git add .` は使わない）

```bash
git add Individuals/
git add docs/records_B59-63/
git add docs/HANDOVER_20260822_v13_21.md
git add docs/precheck_aind_v12.py
git add docs/precheck_corpus_anomalies.py
git add docs/記号対応_20260831/
git add docs/名寄せ_20260901/
git add .gitignore
git rm --cached docs/precheck_aind_v11.py 2>/dev/null; git rm docs/precheck_aind_v11.py 2>/dev/null
```

## 4. 内容の最終確認

```bash
git status --short
git diff --cached --stat | tail -3
```

**期待値**

| 種別 | 件数 |
|---|---:|
| 新規 `Individuals/AIND-*.xml` | 91 |
| 新規 `Individuals/REF_AIND-*.xml` | 3 |
| 修正 `Individuals/*.xml`（既存repoの是正） | 119 |
| 削除 `docs/precheck_aind_v11.py` | 1 |
| 文書類 | 30前後 |

`docs/_backup_*` と `docs/_work` が `??` に**残っていない**ことを確認してください（手順1で ignore 済み）。

## 5. コミット

```bash
git commit -m "B59-63: 校閲完了（実項91/REF化3/重複投入6を除外）+ repo全体是正

- 受領100件 → 重複再投入6件を除外 → 94件を校閲（実項91 / REF化3）
- REF化3件: D03654→D03661 / D03703→D03698 / D03788→D03768
- 新規TMP 33件（P9/N5/L10/I9/O4/T5/S0）
- 裁定 §D-1〜§D-14 を適用（裁定記録_B59-63.md）

## repo全体への初回一括検査・是正（precheck v12）
- 幻番号 167→95 / 汚染注意 144→80 / 毒wd 26→2 / TMP書式不正 8→1
- 汎用置き場化していた番号を解消: TMP-O-00038(21実体) / TMP-L-00014(13実体) / TMP-N-05471(4実体)
- 毒wd Q208507(=The Chemical Brothers)を20箇所で صحيح البخاري に誤用 → Q1023470 に是正
- HANDOVER 懸案『TMP-P-00127 は ابن فهد家 第4のID』は桁落ち誤記と判明し解消

## subtype の向き（§D-13）
- 規約を測定で確定: subtype は「伝主から見た相手方の役割」を表す
- 例外53件を原文と全件照合し、方向19件・ラベル34件を是正（例外0）
- 副産物: 自己ループ2件（D00635/D01335）を原文から解消
- 詳細: 方向再照合_20260901.md

## 作業者向け
- corpus記号の全走査ツール precheck_corpus_anomalies.py を追加
- 13,821項中、作業を止める必要があるのは167項（1.2%）と判明
- 消去対応ルールと連絡文: docs/記号対応_20260831/

- 全1678ファイル well-formed"
```

## 6. push

```bash
git push -u origin B59
```

## 7. PR作成

```bash
gh pr create --base main --head B59 \
  --title "B59-63: 校閲完了（実項91/REF化3）+ repo全体の初回一括是正" \
  --body "詳細は docs/records_B59-63/ を参照。

## 本バッチ
- 受領100件のうち**重複再投入6件**を除外し94件を校閲（実項91 / REF化3 / 新規TMP 33件）
- 裁定 §D-1〜§D-14 を適用

## repo全体への初回一括検査（1678ファイル）
- 幻番号 167→95 / 汚染注意 144→80 / 毒wd 26→2 / TMP書式不正 8→1
- 1番号を多数の別実体に使い回していた3件（O-00038=21実体 / L-00014=13実体 / N-05471=4実体）を解消

## subtype の向き
- 規約を測定で確定し、例外53件を**corpus原文と全件照合**して是正（例外0）
- うち19件は active/passive そのものが逆だったもの

## Waka側に残る作業
- ID-Master への書き戻し（docs/records_B59-63/ID-Master作業指示_20260901.md：既存9行の書換 / 注記2行 / 新規33行の貼付 / precheck v12 差替）
- 未着手の要判断: カテゴリ不一致59件 / TMP-T-00079・TMP-T-00031 / TMP-P-000378〜000383 の連番欠落"
```

`gh` が未設定なら、push 後にブラウザで作成してください。

---

## 補足：今回コミットしないもの

| パス | 理由 |
|---|---|
| `docs/_backup_before_rename_20260901/` | 私の一括修正前のバックアップ。復元用の一時物 |
| `docs/_backup_20260901_direction/` | 方向是正前のバックアップ。同上 |
| `docs/_work/idmaster_0831.tsv` | ID-Master の生データ。過去も repo には入れていません |

全件照合が済んで問題がないと確認できたら、この3つは削除して構いません。
