# ID-Master 作業指示（2026-09-01）

やることは **3つ**です。①②はシートの操作、③はファイルの差し替え。

repo 側の付替え（14ファイル）は **完了済み**なので、シートを直しても幻番号は発生しません。

---

## ① 既存行の書き換え（9行）

シートで **E列（ID）** を検索して行を見つけ、**E列を書き換え**、**F列（Note）の末尾に追記**してください。

| E列で探す | → E列をこれに | F列の末尾に追記する文 |
|---|---|---|
| `TMP-P-000720` | `#AIND-D00900` | `旧TMP-P-000720から統合(B49-53 §D-1)` |
| `TMP-P-000140` | `#AIND-D07182` | `旧TMP-P-000140から統合(B59-63 §A-3)` |
| `TMP-P-000352` | `#AIND-D06936` | `旧TMP-P-000352から統合(B59-63 §A-3)。※#AIND-D08619(メッカ)は別人` |
| `TMP-P-000717` | `#AIND-D04548` | `旧TMP-P-000717から統合(B59-63 §A-3)` |
| `TMP-P-000555` | `#AIND-D09344` | `旧TMP-P-000555から統合(B59-63 §A-3)` |
| `TMP-P-000573` | `#AIND-D10715` | `旧TMP-P-000573から統合(B59-63 §A-3)` |
| `TMP-P-000762` | `#AIND-D05981` | `旧TMP-P-000762から統合(B59-63 §A-3)` |
| `TMP-P-000819` | `#AIND-D07563` | `旧TMP-P-000819から統合(B59-63 §A-3)` |
| `TMP-N-00496` | **`TMP-N-00266`** | `旧TMP-N-00496から統合(異綴)。人物は#AIND-D00900` |

⚠ **F列への追記は必ず行ってください。** 旧番号がシートから消えると、過去の記録から逆引きできなくなります。

⚠ **最下段 `TMP-N-00496` だけは行き先が `TMP-N-00266`** です（ニスバ同士の統合。人物IDには置き換えません）。
あわせて `TMP-N-00266` の **C列（Arabic）** を `الطاووسي | الطاوسي` に。

---

## ② 注記だけ入れる行（2行）

E列は**変更しません**。F列の末尾に追記するだけです。

| E列で探す | F列の末尾に追記する文 |
|---|---|
| `TMP-N-00266` | `ニスバ番号。person位置で使用禁止。人物は#AIND-D00900` |
| `TMP-N-00108` | `ニスバ番号。person位置で使用禁止。人物はTMP-P-000603` |

---

## ③ 新規行の追加（30行）

`TMP登録_B59-63.tsv` を **A列の末尾に貼り付け**。ヘッダ行なし・末尾改行なしです。貼る前に既存末尾行を確認してください。

---

## ④ precheck の差し替え（シート作業ではありません）

`docs/precheck_aind_v11.py` を **`docs/precheck_aind_v12.py`** に差し替えてください（同梱）。
汚染番号リストを **52件 → 67件** に増やしました。追加15件のうち、`TMP-P-000529`（B59-63で単独14回・最大の汚染源）が入っていなかったのが最大の穴です。

> 【補足】当初「ID-MasterのG列に注記を書く」と提案しましたが、**再発防止には効きません**。precheck の汚染リストはスクリプト内にハードコードされていてシートを読まないためです。上の②はあくまで人が後で見るための記録です。

---

## 作業後の確認（任意）

```
python3 docs/precheck_aind_v12.py \
  --xml-dir Individuals --idmaster <ID-MasterのTSV> --out check.md
```
⚠ ID-Master は **TSVに変換**してから渡してください（CSVのままだと全参照が幻番号として偽陽性になります）。

---

## 参考：repo 側で完了済みの付替え（14ファイル）

バックアップは `docs/_backup_before_rename_20260901/` にあります。全1678ファイル well-formed を確認済み。

| 旧番号 | 新ID | ファイル |
|---|---|---|
| `TMP-P-000140` | `#AIND-D07182` | D03634 / D03931 |
| `TMP-P-000352` | `#AIND-D06936` | D00475 / D02626 / D03499 / D03795 |
| `TMP-P-000717` | `#AIND-D04548` | D02659 |
| `TMP-P-000555` | `#AIND-D09344` | D00420 / D01420 / D02097 / D02526 |
| `TMP-P-000573` | `#AIND-D10715` | D00448 |
| `TMP-P-000762` | `#AIND-D05981` | D02762 |
| `TMP-P-000819` | `#AIND-D07563` | D05618 |

置換したのは `active` / `passive` / `ref` / `corresp` の**属性値だけ**です。note 本文中に旧番号を書いている記述はそのまま残してあります（経緯の記録として必要なため）。
