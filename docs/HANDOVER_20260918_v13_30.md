# HANDOVER v13.30 — AINet-DB al-Ḍawʾ al-Lāmiʿ 校閲 (引き継ぎ)

最終更新: 2026-09-18（B87 校閲完了版、裁定は未反映）/ 前任: Claude Opus 5（前提作業＋B86＋B87）← Claude Sonnet 5(B56-58, B84-85) ← claude-fable-5-1(B64-67, B68, B69-73, B74-78, B79-83) ← claude-opus-5(B54, B59-63) ← claude-opus-5(B49–B53) ← claude-fable-5(B20–B48) ← Claude Opus 4.8(B8–B19)
このファイルを次セッションの Claude が最初に読むこと。**v13.29 の統一規則1〜19 はすべて有効**（本ファイルは差分）。v13.29 は `docs/HANDOVER_20260918_v13_29.md`。

## v13.29 → v13.30 の変更点（2026-09-18）

### 1. B87 の処理結果

- 受領: `B86.zip` 内の `B87/` フォルダ **77件**（`AIND-D04468`〜`D04507` / `D07313`〜`D07382` / `D07671`〜`D07696`）。
- **重複投入ゼロ**（AIND番号・12桁ID の両方で `Individuals/` と突合）、77件すべてが corpus に**ちょうど1回ずつ**存在。`$$$` 台帳に既登録のものは無し。
- **実項 69 / REF化 8 = 77件**。REF 8件の転送先はすべて corpus 内で一意に閉合:
  `D04470`→`#AIND-D04465` / `D04503`→`#AIND-D04514` / `D07323`→`#AIND-D08818` / **`D07325`→`#AIND-D07764`**（検証パスが是正） / `D07344`→`#AIND-D03619` / `D07679`→`#AIND-D10951` / `D07683`→`#AIND-D07668` / `D07684`→`#AIND-D06693`。
  `D07686`（محمد بن عبد العزيز زعيم تونس）は「وأظنه الماضي فيمن جده…」が推測の同定コメントであり転送指示ではないため**実項**とし、`#AIND-D07668` への `possible_identity`（cert medium）とした。
- **$$$ 帳簿 1477 → 1485**（corpus txt の8行を `$# $` → `$# $$$` に更新。バックアップ `docs/_work/corpus_backup_before_B87.txt`、差分は当該8行のみ）。
- **新規 TMP 50件**: P-001115〜001144（30）/ N-05514（1）/ L-00274〜00276（3）/ I-00163〜00165（3）/ O-00270〜00277（8）/ T-00178〜00182（5）。S の新規なし。一覧は `docs/records_B87/TMP登録_B87.tsv`（7列ヘッダなし）、仮番号→実番号の対応は `docs/_work/B87_incoming/tmp_map.json`。
  - **取り下げ2件**: 仮番号 `TMP-P-NEW87b-02`（→ `#AIND-D02520`）・`TMP-P-NEW87c-07`（→ `#AIND-D03146`）。いずれも corpus 本伝を見落として発行しかけたもので、**実採番していない＝欠番は生じていない**。
- 作業方式: **4チャンク並行（各19〜20件、claude-fable-5）＋独立検証パス2本（Opus 5: 内容検証 / 機械検査）**。ブリーフは `docs/records_B87/CHUNK_BRIEF_B87.md`（統一規則1〜19を転記＋本バッチ固有の注意）。
- **`Individuals/` へ統合済み: 2,359 → 2,436件**（うち REF 152 → 160件）。全2,436件 well-formed。

### 2. 検証パスの結果

**内容検証（`claude-b87-verify-content`）**: H3件・M8件・L7件。H はいずれも **「corpus 本伝の見落とし」という同一の失敗型**（統一規則10 が警告している型）で、その場で修正済み。

- **H-1** `REF_AIND-D07325` の転送先が誤り。`#AIND-D07851`（محمد بن عبد الله بن **يوسف** بن حجاج **بن قريش** خادم شيخنا）ではなく **`#AIND-D07764`**（175 محمد بن عبد الله بن حجاج بدر الدين البرماوي الأصل القاهري **الماضي أبوه**）。転送指示「في ابن عبد الله بن حجاج」と逐語一致し、祖父名アルファベット順の正位置（D07763 بن جار الله → D07764 بن حجاج → D07765 بن حسن）にあり、父 `#AIND-D04437`（**والد البدر محمد الآتي**）と双方向閉合する。**本引き継ぎ担当が corpus で再確認し、是正を正当と確認した。**
- **H-2** `AIND-D07334` の父を `#AIND-D02520`（الحسن بن أحمد بن محمد بن سلامة بن عطوف بن يعلى البدر السلمي المكي البزار）に是正。ナサブ6代＋ニスバ2つが完全一致。**再確認済み。**
- **H-3** `AIND-D07375` の兄弟 سليمان を `#AIND-D03146`（995 سليمان بن الخواجا داود بن علي بن بهاء الكيلاني المكي … مات باسكندرية في طاعون سنة اثنتين وأربعين）に是正。父名・ニスバ・没地・没年が完全閉合。**再確認済み。**
- **違反ゼロと確認された項目**: 日付78組（h2g.py でグレゴリオ暦再計算、`@when` へのヒジュラ年混入ゼロ）／relation 方向179件（B86 の誤り型の再発なし）／実項69件に見落とされた転送指示ゼロ／AIND 同定48件すべて相手側 corpus 本文で裏取り／en 訳への日本語混入ゼロ・立項連番混入ゼロ・「ビン」残存ゼロ／統一規則16 の無断地名付与ゼロ。

**機械検査（`claude-b87-verify-mech`）**: 検査13項目、違反2件（いずれも自動修正）、残件ゼロ。

- 自動修正: `REF_AIND-D07344` の note 順序（`source → translation(ja) → translation(en) → reference` に揃えた）／`TMP_NEW_B87c.tsv` の列数補填。
- **0件だった項目**: well-formed 77/77 ／ `relation/@n` の規約A違反 0（親族106件に `@n` 無し・非親族73件が1起点連番）／ 原文 `note type="source"` 77/77 かつ corpus と正規化後完全一致（他項本文・`###$`・`$$$`・立項連番の混入なし）／ 初版 respStmt の改変・削除 0 ／ ID 形式・`xml:lang="lat"`・`<desc>` の `xml:lang` 違反 0 ／ **幻番号 0**（AIND 161件・TMP 136件を実在確認）／ 毒wd の属性値残存 0 ／ affiliation・state 二重符号化 0 ／ `@when` へのヒジュラ混入 0 ／ **仮TMP のチャンク間重複 0**（ファジー15組はすべて別人・別地名）／ 仮TMP の TSV↔XML 片側漏れ 0（50↔50）／ REF 8件の形式が既存 REF 152件と完全一致。
- 検査スクリプトは `docs/_work/B87_incoming/verify/` に保存。

### 3. 裁定待ち（`docs/要判断_B87.md`、**51項目**）

延べ57件の提起（§Da 12 / §Db 12 / §Dc 11 / §Dd 14 / 検証M 8）を**重複12件分を統合して51項目**に整理した。★裁定必須 33 / 確認のみ 18。

- **§A（裁定不要・作業依頼3件）**: 新規TMP 50件の ID-Master 貼付／取り下げ2件の記録／`TMP-L-00234` の Note 修正＋別名追加。**あわせて B86 分8件・前バッチ裁定分12件の貼付状況も要確認**（B87 の採番は 2026-09-18 12:36 のシート実測 max+1 で行っており、B86 分8件は貼付済みであることを確認済み）。
- **§B 要照合 wd/gn 3件**（こちらから Wikidata / GeoNames は参照できない）。
- **§C 名寄せ・repo 是正 9件**。最大は **`TMP-P-000010`（العز بن جماعة）→ `#AIND-D07198`**（repo 9ファイル・10箇所に波及）。他に `TMP-P-000158`→`#AIND-D13339`（実測5ファイル5箇所）、`TMP-P-001080`→`#AIND-D07896`、`TMP-P-000038`→`#AIND-D10240`、`TMP-P-000548`→`#AIND-D07789`。
- **§D 個別裁定 33件**。
- **§E 統一規則の候補 6件（規則20〜25）** — ★これが後続バッチの作業方針を止めている:
  - **§E-1 حج event の `placeName مكة`**: 3系統が独立提起。repo 全体 75件中74件が保持。「認めない」裁定なら74件の一括除去が発生する。集約担当の推奨は**例外として認める**。
  - **§E-2「بها」照応による地名**・**§E-3 統一規則16の適用範囲**: B87c だけが厳格運用（ニスバ・施設・職から推定された placeName を一律除去）、他3チャンクは保持側。**B88 以降で同じ論点が毎バッチ再燃するので早期の裁定が望ましい**。
  - §E-4 他項からの情報補完の可否、§E-5 `event/@n` の扱い（repo 既存1,253ファイル中211ファイルが type ごとの振り直し。規則19 は relation 限定なので現状は違反ではない）、§E-6。
- **repo への波及が判明している未修正の汚染**（§C に記載）: 既存1ファイルで「المصابيح」に `TMP-T-00081`（المنهاج الفرعي）が流用されている。また `#AIND-D04089`（ابن عليبة の عبد القادر）が B87 の2箇所で別人に流用されかけていた（本バッチ内では是正済み）。

### 4. 統一規則の変更

**本バッチでの新規追加なし**（1〜19 のまま）。規則20以降の候補は `docs/要判断_B87.md` §E に整理してある。**裁定後に本引き継ぎの規則節へ転記すること。**

運用上の補足（規則ではないが次バッチのブリーフに入れること）:
- **corpus の本文抽出では `$$$` 台帳行を境界として扱う**。`###$AIND-Dxxxxx | 12桁$#` だけを境界にすると、直後の `###$<12桁>$# $$$ …`（AIND番号を持たない転送見出し行）を本文に取り込んでしまう。B87 では抽出 JSON の5件（D07315 / D07326 / D07357 / D07370 / D07382）が該当した（各チャンクの `<note type="source">` には混入せず、機械検査で corpus と完全一致を確認済み）。
- **ID-Master は Google Drive コネクタで直接落とすのが確実**（`mcp__Google_Drive__download_file_content` に `exportMimeType: text/csv`、fileId `1MSwfebHM1Ak39Qqk7ZMrFhoHhE4COxd9PyQs2tTujuk`）。ブラウザ経由の XHR は不要。B87 では `docs/_work/idmaster_working_20260918.tsv` として保存した（7,680データ行・重複IDゼロ）。

## 次回運用

- **次回の対象は B88（159件）**。zip は受領済み（`B86.zip` 内の `B88/` フォルダ）。
- 着手時チェックリスト: ①本ファイル＋v13.29 を読む ②**ID-Master を Drive コネクタで再取得**（並行追加・B87 分50件の貼付状況を必ず確認）③`docs/要判断_B87.md` の裁定状況を確認（特に §E の統一規則候補。未裁定ならブリーフに「B87 と同じ暫定運用」と明記して割れを防ぐ）④重複投入チェック（AIND番号＋12桁ID）⑤precheck 実行。
- **モデル配分は B87 と同じで良好だった**: チャンク校閲を claude-fable-5（4本並行）、検証パスを Opus 5（内容検証・機械検査の2本）。159件なら **8チャンク×約20件＋検証2本**が妥当。Fable は原文の読解・同定でよく働き、Opus の内容検証が本伝見落とし3件を捕捉した。この組み合わせを維持することを推奨する。
- ブリーフには本バッチと同様に**汚染番号の一覧・毒wd・REF 有力候補・チャンク固有の注意**を必ず入れること（B87 では precheck 37件がほぼそのままブリーフの注意書きになり、見落としを防いだ）。

## 採番起点（次バッチ B88〜）★着手時に ID-Master 実測 max+1 で必ず再確認・並行消費注意★

2026-09-18 時点（B86 の8件＋B87 の50件を反映後の想定値。**B87 分50件は Waka 未貼付**）:
**TMP-P 次は 001145**（001112 は B86 での取り下げによる欠番。再利用しない） / **TMP-N 次は 05515** / **TMP-L 次は 00277** / **TMP-I 次は 00166** / **TMP-O 次は 00278** / **TMP-T 次は 00183** / **TMP-S 次は 00074（未使用のまま）**。

## 現在の repo 状態

- `Individuals/` **2,436件**（うち REF **160件**）。全件 well-formed・全件が原文 `<note type="source">` を保持。
- corpus `$$$` = **1485**。
- AINet Browser は **v15 = 2,359件**のまま（**B87 の77件は未反映**。`docs/browser/build.py` の再実行と Artifact への再公開が必要）。
- 本セッションの成果は `origin/main` から切ったブランチ **`B87-20260918`** に載せてある。**push・PR は Waka**。手順は `docs/git手順_B86.md` に準じる。
